import React from 'react';

const NavBar = () => {
    return (
        <nav>
            <div className="logo">Logo</div>
            <div clasName="nav-links">
                <a href="#">Login</a>
                <a href="#">Sign up</a>
            </div>
        </nav>
    )
}

export default NavBar;